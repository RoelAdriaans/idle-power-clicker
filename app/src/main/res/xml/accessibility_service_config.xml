<?xml version="1.0" encoding="utf-8"?>
<!--
    Configuration for the Accessibility Service.
    canPerformGestures="true" is the key flag that lets us inject swipes.
    We don't listen to any accessibility events — the service is purely
    used as a gesture-injection channel.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity="com.example.idlepowerhelper.MainActivity" />
